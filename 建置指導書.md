# LINE BOT × OpenClaw AI Agent 建置指導書

## 📚 目錄
1. [前置要求](#前置要求)
2. [第一步：環境準備](#第一步環境準備)
3. [第二步：取得憑證](#第二步取得憑證)
4. [第三步：安裝依賴](#第三步安裝依賴)
5. [第四步：配置環境變數](#第四步配置環境變數)
6. [第五步：啟動應用](#第五步啟動應用)
7. [第六步：設置 LINE Webhook](#第六步設置-line-webhook)
8. [第七步：測試](#第七步測試)
9. [故障排除](#故障排除)

---

## 前置要求

在開始建置前，請確保您有：

- ✅ GitHub Codespaces 帳戶（或本地 Linux/Mac 環境）
- ✅ LINE 開發者帳戶
- ✅ Groq API 帳戶（免費）
- ✅ 基本的命令列知識

### 所需时间：30 分鐘

---

## 第一步：環境準備

### 1.1 克隆或進入專案

如果您還沒有進入專案目錄，請執行：

```bash
cd /workspaces/my-openclaw-agent
```

或者克隆專案：

```bash
git clone https://github.com/iotschool111ab/my-openclaw-agent.git
cd my-openclaw-agent
```

### 1.2 確認 Python 版本

確保您的環境中有 Python 3.8 以上：

```bash
python --version
# 或
python3 --version
```

預期輸出應類似：
```
Python 3.12.1
```

### 1.3 檢查已有檔案

驗證專案中已有以下結構：

```bash
ls -la
```

您應該看到：
```
.env                  # 環境變數檔
.gitignore           # Git 忽略檔案
requirements.txt     # Python 依賴清單
main_simple.py       # 簡單版應用
run_simple.sh        # 運行腳本
README.md            # 主要說明文件
```

---

## 第二步：取得憑證

### 2.1 LINE Bot 憑證

#### 步驟 A：建立 LINE Developers 帳戶

1. 訪問 [LINE Developers](https://developers.line.biz/)
2. 點擊 **Start now** 或登入
3. 使用 LINE 帳號登入或建立新帳號

#### 步驟 B：建立 Message API Channel

1. 進入 Developers Console
2. 點擊 **Create new channel** 或 **新建頻道**
3. 選擇 **Messaging API**
4. 填入以下資訊：
   - **Channel name**：例如 "OpenClaw AI BOT"
   - **Channel description**：例如 "AI 助手 BOT"
   - **Category**：選擇合適的分類
   - **Subcategory**：選擇 "Bot"
5. 同意協議後點擊 **Create**

#### 步驟 C：取得 Channel Credentials

1. 進入剛建立的 Channel
2. 進入 **Messaging API** 設定
3. 找到以下資訊：
   - **Channel Access Token**：長字符串起始
   - **Channel Secret**：32 位十六進制字符串

✅ **保存這兩個值**，您將在第四步中使用

### 2.2 Groq API 金鑰

#### 步驟 A：建立 Groq 帳戶

1. 訪問 [Groq Console](https://console.groq.com/)
2. 點擊 **Sign Up**
3. 使用 Google 帳號或郵箱建立帳戶
4. 驗證郵箱

#### 步驟 B：建立 API 金鑰

1. 登入 Groq Console
2. 進入 **Keys** 或 **API Keys** 頁面
3. 點擊 **Create API Key**
4. 選擇 **API Key**
5. 複製生成的金鑰（形式：`gsk_...`）

✅ **保存此金鑰**，您將在第四步中使用

---

## 第三步：安裝依賴

### 3.1 更新 pip（可選但推薦）

```bash
pip install --upgrade pip
```

### 3.2 安裝項目依賴

```bash
pip install -r requirements.txt
```

**預期安裝的套件：**
- `flask` - Web 框架
- `line-bot-sdk` - LINE Bot SDK
- `requests` - HTTP 客戶端
- `python-dotenv` - 環境變數管理

**驗證安裝成功：**

```bash
python -c "import flask; import linebot; import requests; print('✅ All dependencies installed!')"
```

預期輸出：
```
✅ All dependencies installed!
```

### 3.3 檢查版本（可選）

```bash
pip list | grep -E "flask|line-bot-sdk|requests|python-dotenv"
```

---

## 第四步：配置環境變數

### 4.1 編輯 .env 檔案

使用您選擇的編輯器打開 `.env` 檔案：

```bash
code .env
# 或使用其他編輯器
```

### 4.2 填入憑證

將以下範本中的值替換為您在第二步獲得的憑證：

```env
# LINE BOT Configuration
LINE_CHANNEL_ACCESS_TOKEN=您的_Channel_Access_Token
LINE_CHANNEL_SECRET=您的_Channel_Secret

# Groq Configuration (免費 API)
GROQ_API_KEY=您的_Groq_API_Key

# OpenClaw Configuration (if needed)
OPENCLAW_GATEWAY_URL=ws://localhost:8080
```

**完整範例（不含實際金鑰）：**

```env
LINE_CHANNEL_ACCESS_TOKEN=g7HmEn3OjyYVHzlCkXkYO1pJFH7fRzCL...
LINE_CHANNEL_SECRET=aea78532ac3a816e70a5f4e28e16f8a4
GROQ_API_KEY=gsk_OYF0uq9sVxUkJVOIB0UtWGdyb3...
OPENCLAW_GATEWAY_URL=ws://localhost:8080
```

### 4.3 驗證環境變數

```bash
python check_setup.py
```

**預期輸出：**

```
🔍 Checking LINE BOT + OpenClaw setup...

✅ Environment variables are set.
✅ OpenClaw CLI is available.

🎉 Setup looks good! You can now run: python main.py
Remember to set your webhook URL in LINE Developers Console to your app's /callback endpoint.
```

如果出現錯誤，請檢查 `.env` 檔案中的值是否正確。

---

## 第五步：啟動應用

### 5.1 運行簡單版本（推薦）

簡單版本直接使用 Groq API，無需複雜的 OpenClaw 配置：

```bash
./run_simple.sh
```

### 5.2 預期輸出

```
🚀 Starting simple LINE BOT server with direct Groq API...
 * Serving Flask app 'main_simple'
 * Debug mode: off
 * Running on all addresses (0.0.0.0)
 * Running on http://127.0.0.1:5000
 * Running on http://10.0.0.165:5000
Press CTRL+C to quit
```

✅ **應用已成功啟動！** 應用正在 `http://localhost:5000` 運行

### 5.3 另一個終端中驗證

如果您想驗證服務是否運行，在新終端中執行：

```bash
curl -X GET http://localhost:5000/
```

預期會有 404 錯誤（因為 GET /路徑未定義），但這表示服務已在運行。

---

## 第六步：設置 LINE Webhook

### 6.1 確認應用運行狀態

確保 `./run_simple.sh` 仍在運行（應該看到終端中有 Flask 日誌）

### 6.2 獲取 Codespaces 公開 URL

在 VS Code 中：

1. 點擊左下角 **Ports** 或按 `Ctrl+Shift+;`
2. 找到 **5000** 端口
3. 右鍵點擊 → **Copy Forwarded Address**
4. 或尋找類似這樣的 URL：
   ```
   https://shiny-meme-g94pjwv4jxvh9665-5000.app.github.dev
   ```

### 6.3 構建 Webhook URL

您的完整 Webhook URL 應為：

```
https://shiny-meme-g94pjwv4jxvh9665-5000.app.github.dev/callback
```

（其中 `shiny-meme-g94pjwv4jxvh9665` 是您的 Codespaces 識別碼）

### 6.4 在 LINE Developers Console 設置

1. 訪問 [LINE Developers Console](https://developers.line.biz/)
2. 選擇您的 Channel
3. 進入 **Messaging API 設定**（通常在 **Settings** 或 **設定** 標籤）
4. 尋找 **Webhook URL** 字段
5. 粘貼您的完整 URL（包括 `/callback`）
6. 點擊 **驗證** 或 **Verify** 按鈕
7. 看到 "Success" ✅ 確認驗證成功
8. 啟用 Webhook：
   - 找到 **Use webhook** 或 **Webhook 使用**
   - 改為 **Enabled** 或 **啟用**
9. 點擊 **Save** 或 **保存**

**預期成功畫面：**
```
Webhook URL verification: Success
```

---

## 第七步：測試

### 7.1 新增 LINE 好友

1. 在 LINE Developers Console 取得您的 **Bot ID**
2. 或掃描 Channel 上的 QR Code 加入 Bot

### 7.2 傳送測試訊息

在 LINE 中傳送一條簡單的訊息給您的 Bot，例如：
```
Hello
```

### 7.3 查看回應

您的 Bot 應該回覆您一個由 Groq AI 生成的回應，例如：
```
How can I assist you today?
```

### 7.4 測試多種訊息

嘗試傳送不同類型的訊息：

| 訊息類型 | 範例 | 預期 |
|---------|------|------|
| 簡單文字 | "Hello" | AI 回應 |
| 問題 | "如何學習 Python?" | AI 回應答案 |
| 圖片 | 上傳圖片 | BOT 確認收到 |
| 文件 | 上傳檔案 | BOT 確認收到 |

### 7.5 檢查日誌

在運行應用的終端中，您應該看到日誌：

```
127.0.0.1 - - [03/May/2026 04:15:32] "POST /callback HTTP/1.1" 200 -
```

這表示 LINE 已成功發送訊息到您的 Webhook。

---

## 故障排除

### ❌ 問題 1：無法啟動應用

**錯誤訊息：** `ModuleNotFoundError: No module named 'flask'`

**解決方案：**
```bash
pip install -r requirements.txt
```

---

### ❌ 問題 2：Webhook 驗證失敗

**錯誤訊息：** "Webhook URL verification failed"

**可能原因和解決方案：**

1. **URL 不正確**
   - 確保 URL 包含 `/callback`
   - 檢查複製時是否有多餘空格
   
2. **應用未運行**
   - 確保 `./run_simple.sh` 仍在運行
   - 檢查應用日誌是否有錯誤

3. **Codespaces URL 已改變**
   - 重新取得當前的轉發 URL
   - 更新 LINE Console 中的設定

4. **應用在啟動時出錯**
   ```bash
   python check_setup.py
   ```
   確認所有環境變數都正確設置

---

### ❌ 問題 3：無法接收訊息

**現象：** 傳送訊息到 BOT 但沒有回應

**檢查清單：**

1. 確認 Webhook 已啟用
   ```
   LINE Console → Messaging API → Webhook 使用 → 啟用
   ```

2. 查看應用日誌是否收到 POST 請求
   - 應該看到 `"POST /callback HTTP/1.1" 200`

3. 檢查環境變數
   ```bash
   python check_setup.py
   ```

4. 重新啟動應用
   ```bash
   # 按 Ctrl+C 停止當前應用
   # 重新運行
   ./run_simple.sh
   ```

---

### ❌ 問題 4：Groq API 限制

**錯誤訊息：** "Request too large" 或 "Rate limit exceeded"

**原因：** 免費層有 6000 tokens/minute 的限制

**解決方案：**

1. **等待速率限制重置（推薦）**
   - Groq 會在幾分鐘後重置限制

2. **升級到 Groq Dev Tier**
   - 訪問 https://console.groq.com/settings/billing
   - 升級為付費層獲得更高限制

3. **使用完整 OpenClaw 版本**
   ```bash
   ./run.sh
   ```
   （但需要更複雜的配置）

---

### ❌ 問題 5：環境變數未設置

**錯誤訊息：** "Cannot set LINE_CHANNEL_ACCESS_TOKEN: Invalid identifier"

**原因：** `.env` 檔案中有註解或特殊字符

**解決方案：**

1. 編輯 `.env` 檔案
2. 確保每一行都是有效的格式：
   ```
   KEY=VALUE
   ```
3. 避免行內註解（註解必須在新行開頭）
4. 保存後重新啟動應用

---

## ✅ 驗證清單

在認為建置完成前，請確認以下所有項目：

- [ ] Python 3.8+ 已安裝
- [ ] 所有依賴已安裝（執行 `pip install -r requirements.txt`）
- [ ] `.env` 檔案中填入了正確的憑證
- [ ] 執行 `python check_setup.py` 顯示 ✅
- [ ] 應用已啟動（執行 `./run_simple.sh`）
- [ ] 取得了 Codespaces 的轉發 URL
- [ ] Webhook URL 已在 LINE Console 中設置並驗證成功
- [ ] Webhook 已啟用
- [ ] 能夠在 LINE 中傳送訊息並收到 BOT 回應

---

## 📞 常用命令參考

### 安裝和環境

```bash
# 安裝依賴
pip install -r requirements.txt

# 檢查設置
python check_setup.py

# 列出已安裝的套件
pip list
```

### 運行應用

```bash
# 運行簡單版本（推薦）
./run_simple.sh

# 運行完整版本
./run.sh

# 直接運行 Python
python main_simple.py
```

### 調試

```bash
# 測試 Groq API
python -c "
import requests
import os
from dotenv import load_dotenv
load_dotenv()
headers = {'Authorization': f'Bearer {os.getenv(\"GROQ_API_KEY\")}', 'Content-Type': 'application/json'}
data = {'model': 'llama-3.1-8b-instant', 'messages': [{'role': 'user', 'content': 'test'}], 'max_tokens': 100}
r = requests.post('https://api.groq.com/openai/v1/chat/completions', headers=headers, json=data)
print(r.status_code, r.json() if r.status_code == 200 else r.text)
"

# 查看應用日誌
tail -f /tmp/app.log  # 如果有的話
```

### Git 操作

```bash
# 查看改變的檔案
git status

# 提交改變
git add .
git commit -m "Setup LINE BOT with Groq API"

# 推送到遠端
git push origin main
```

---

## 🎓 架構簡介

### 系統流程

```
使用者 (LINE)
    ↓
LINE Server
    ↓
Webhook URL (您的應用)
    ↓
Flask 應用 (main_simple.py)
    ↓
Groq API
    ↓
AI 回應
    ↓
返回給使用者 (LINE)
```

### 檔案結構

```
my-openclaw-agent/
├── main_simple.py          # 簡單版應用 (直接調用 Groq API)
├── main.py                 # 完整版應用 (使用 OpenClaw)
├── run_simple.sh           # 運行簡單版本的腳本
├── run.sh                  # 運行完整版本的腳本
├── check_setup.py          # 設置檢查腳本
├── requirements.txt        # Python 依賴
├── .env                    # 環境變數（本地，不提交到 Git）
├── .gitignore             # Git 忽略規則
├── README.md              # 簡介文件
└── 建置指導書.md           # 本檔案
```

---

## 🚀 下一步

### 功能擴展

現在您已成功建置基本的 BOT，可以考慮：

1. **增強功能**
   - 新增圖片識別功能
   - 新增文件處理功能
   - 新增多語言支持

2. **改進部署**
   - 使用 Docker 容器化應用
   - 部署到雲端平台（Heroku、AWS 等）
   - 實現持續部署（CI/CD）

3. **知識整合**
   - 連接數據庫儲存對話歷史
   - 實現用戶個性化回應
   - 新增自學習功能

### 有用的資源

- 📖 [LINE Bot SDK 文檔](https://github.com/line/line-bot-sdk-python)
- 📖 [Groq API 文檔](https://console.groq.com/docs)
- 📖 [Flask 文檔](https://flask.palletsprojects.com)
- 📖 [OpenClaw 文檔](https://docs.openclaw.ai)

---

## 📧 支持

如果遇到問題無法解決：

1. 查看本文件的「故障排除」章節
2. 檢查應用日誌中的錯誤信息
3. 驗證所有環境變數和憑證是否正確
4. 查看官方文檔和社區

---

## 版本信息

- **建置日期**：2026-05-03
- **OpenClaw 版本**：2026.5.2
- **Python 版本**：3.12+
- **Groq API**：最新版本
- **LINE Bot SDK**：3.23.0

---

**祝您使用愉快！🎉**

如有任何問題，請隨時查閱本指導書或聯繫開發團隊。
