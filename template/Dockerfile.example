# 生成项目的 Dockerfile 模板

将此文件保存为 `Dockerfile` 在项目根目录。

## 使用方法

```dockerfile
FROM python:3.12-slim

WORKDIR /app

# 安装依赖
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

# 复制应用
COPY . .

# 健康检查
HEALTHCHECK --interval=30s --timeout=10s --start-period=5s --retries=3 \
    CMD curl -f http://localhost:${PORT:-5000}/health || exit 1

# 启动应用
CMD ["gunicorn", "-b", "0.0.0.0:${PORT:-5000}", "-w", "4", "main:app"]
```

## 构建和运行

```bash
# 构建镜像
docker build -t my-ai-bot .

# 运行容器
docker run -p 5000:5000 \
  -e PLATFORM=line \
  -e PLATFORM_TOKEN=your_token \
  -e AI_API_KEY=your_key \
  my-ai-bot
```

## Docker Compose 示例

```yaml
version: '3.8'

services:
  app:
    build: .
    ports:
      - "5000:5000"
    environment:
      - PLATFORM=line
      - PLATFORM_TOKEN=${PLATFORM_TOKEN}
      - PLATFORM_SECRET=${PLATFORM_SECRET}
      - AI_API_KEY=${AI_API_KEY}
      - DEBUG=false
      - LOG_LEVEL=INFO
    volumes:
      - ./logs:/app/logs
    restart: unless-stopped
```

运行：
```bash
docker-compose up -d
```
